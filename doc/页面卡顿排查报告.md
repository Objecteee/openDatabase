# 页面卡顿 / 开发者工具打不开 - 排查报告

> 排查时间：2025-03-10

---

## 一、已确认正常项

| 项目 | 结果 |
|------|------|
| 后端 server | ✅ 运行在 3000 端口 |
| Vite 响应 | ✅ localhost:5175 返回 HTTP 200，HTML 加载约 6ms |
| main.tsx 编译 | ✅ 约 1.7ms |
| 代理配置 | ✅ vite proxy 指向 3000 |

---

## 二、发现的问题

### 2.1 项目路径含特殊字符

**路径**：`/Users/duole-06/Desktop/prompt - 副本`

- 含**空格**和**中文「副本」**
- 进程列表中显示为乱码：`prompt - M-eM^IM-/M-fM^\M-,/`
- 可能导致：Vite 文件监听异常、模块解析错误、Source Map 错乱

### 2.2 多个 Vite 实例并存

| 端口 | 项目 | 说明 |
|------|------|------|
| 5173 | miniProject | 其他项目 |
| 5174 | (未知) | 占用 |
| 5175 | prompt - 副本 | 当前项目（因 5173/5174 被占而使用） |

多实例可能增加内存和 CPU 占用。

### 2.3 首屏依赖体积

- `index` 主包：~233 KB
- `ChatPage`（懒加载）：~160 KB
- `embedding.worker`：~880 KB（仅访问文档库时加载）
- `ort-wasm`：**~21.5 MB**（Transformers.js 的 ONNX 运行时，仅文档库加载）

---

## 三、建议处理步骤

### 步骤 1：项目移到纯英文路径（优先尝试）

```bash
# 复制到无空格、无中文的路径
cp -r "/Users/duole-06/Desktop/prompt - 副本" ~/Desktop/prompt-copy
cd ~/Desktop/prompt-copy/client
npm install
chmod -R +x node_modules/.bin/
npm run dev
```

然后访问 `http://localhost:5173`（或终端显示的端口）。

### 步骤 2：关闭其他 Vite 项目

- 关闭 miniProject 等占用 5173/5174 的 `npm run dev`
- 仅保留当前项目的 dev 进程

### 步骤 3：用无痕模式 + 单标签页测试

- 打开 Chrome 无痕窗口
- 只保留一个标签页
- 访问 `http://localhost:5175`

### 步骤 4：换浏览器验证

- 若 Chrome 卡死，可尝试 Safari 或 Edge

---

## 四、若仍卡顿，可做的代码级排查

1. **临时去掉懒加载**：在 `App.tsx` 中改回直接 `import` ChatPage，确认是否与懒加载有关。
2. **最小化入口**：在 `index.html` 中写死静态内容，不加载 React，确认是否为框架或依赖导致。
3. **检查 React 19**：可尝试降级到 React 18，排除版本兼容问题。

---

## 五、结论

当前最可疑的是**项目路径中的空格和中文**，建议先按步骤 1 将项目移到 `~/Desktop/prompt-copy` 再测试。
