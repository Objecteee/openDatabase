# Omni-Knowledge Base 项目进度与待办清单

> 基于 main.mdc 规则与现有实现，整理已完成任务与待办事项。  
> 更新日期：2025-03-10

---

## 一、项目概述

全栈知识库应用：文档上传 → 解析 → 切片向量化 → RAG 对话。技术栈：React 19 + Vite + Tailwind、Node.js Express、Supabase（pgvector、Storage）。

---

## 二、已完成任务

### 2.1 文档上传（完整实现 ✅）

| 功能 | 实现位置 | 说明 |
|------|----------|------|
| 秒传检查 | `POST /api/documents/check-upload` | 按 hash 查询已有文档，命中则直接返回 id |
| 小文件直传 | `POST /api/documents/upload` | < 5MB 使用 FormData 直传 |
| 大文件分片上传 | `init` → `status` → `chunk` × N → `complete` | ≥ 5MB 分片 2MB/片，并发 6 |
| 断点续传 | `GET /api/documents/upload/status/:upload_id` | 查询已传分片，仅上传缺失分片 |
| MD5 Web Worker | `client/src/workers/hash.worker.ts` | 分块计算，不阻塞主线程 |
| 上传核心逻辑 | `client/src/lib/uploadCore.ts` | 秒传、直传、分片流程串联 |
| 多文件上传 UI | `MultiFileUploadZone`、`useMultiFileUpload` | 拖拽、进度、并发控制 |

**规范符合**：main.mdc 要求 MD5 在 Worker、并发 6、断点续传、大文件分片均已满足。

---

### 2.2 Supabase 数据层（已接入 ✅）

| 模块 | 路径 | 说明 |
|------|------|------|
| Supabase 客户端 | `server/src/lib/supabase.ts` | 环境变量配置 |
| documentService | `server/src/services/documentService.ts` | createDocument、findByHash、getDocumentById、updateDocumentStatus、deleteDocument |
| chunkService | `server/src/services/chunkService.ts` | insertChunks、deleteChunksByDocumentId、searchChunks（向量检索） |
| conversationService | `server/src/services/conversationService.ts` | createConversation、getConversations、updateConversationTitle、deleteConversation |
| messageService | `server/src/services/messageService.ts` | createMessage、getMessagesByConversation |
| documents 路由 | `server/src/routes/documents.ts` | 完整 CRUD + 上传 + 解析 + chunks 写入 |

**注意**：conversationService、messageService 已实现，但 **conversations 路由未挂载**，Chat 未接入。

---

### 2.3 本地 Embedding（完整实现 ✅）

| 模块 | 路径 | 说明 |
|------|------|------|
| embedding Worker | `client/src/workers/embedding.worker.ts` | Transformers.js 单例加载，embed、embedBatch |
| embeddingClient | `client/src/lib/embeddingClient.ts` | 单例 Worker 管理、initEmbedding、subscribeEmbedding |
| useEmbeddingModel | `client/src/hooks/useEmbeddingModel.ts` | 进入文档页自动 init，返回 isReady、progress、error |
| 模型 | Xenova/paraphrase-multilingual-MiniLM-L12-v2 | 384 维，中文支持好 |

**规范符合**：单例模式、Web Worker 离线化、不阻塞主线程。

---

### 2.4 文档解析与切片向量化（txt/md ✅）

| 模块 | 路径 | 说明 |
|------|------|------|
| textParser | `server/src/parsers/textParser.ts` | txt/md 按段落 + 固定长度切片 |
| parseService | `server/src/services/parseService.ts` | 从 Storage 下载并解析 |
| 解析接口 | `GET /api/documents/:id/parse` | 返回 chunks 供前端 embed |
| chunks 写入 | `POST /api/documents/:id/chunks` | 接收 embedding 写入 chunks 表 |
| vectorizeService | `client/src/lib/vectorizeService.ts` | parse → embedBatch → post 串联 |
| DocumentList | `client/src/components/DocumentList.tsx` | 「向量化」按钮，仅 txt/md 显示 |

**数据库要求**：chunks.embedding 须为 vector(384)，需执行迁移 SQL（见 `doc/切片向量化实现说明.md`）。

---

### 2.5 Chat 对话页（基础实现 ✅）

| 功能 | 实现 | 说明 |
|------|------|------|
| SSE 流式输出 | `POST /api/chat` | 302.ai Chat API，stream: true |
| 打字机效果 | requestAnimationFrame | 符合 main.mdc 渲染优化 |
| Markdown 渲染 | ReactMarkdown + remarkGfm | 标题、列表、代码块等 |
| 布局 | Layout + 对话/文档库导航 | 路由 `/`、`/documents` |

**当前限制**：Chat 为「无状态」模式，不绑定 conversation_id，不写入 messages 表，不接入 RAG。

---

### 2.6 其他

- **文档列表**：GET /api/documents，支持删除、预览（signed URL）、向量化
- **AI Provider**：302.ai Chat API 封装，支持 SSE
- **常量一致性**：client/constants/upload.ts、server/constants/upload.ts 对齐 CHUNK_SIZE、SMALL_FILE_THRESHOLD

---

## 三、待完成任务

### 3.1 高优先级（核心链路）

#### 1. 挂载 conversations 路由并接入 Chat

| 任务 | 说明 |
|------|------|
| 新建 `server/src/routes/conversations.ts` | POST /、GET /、GET /:id/messages、PATCH /:id、DELETE /:id |
| 在 `server/src/index.ts` 挂载 | `app.use("/api/conversations", conversationsRouter)` |
| 改造 `server/src/routes/chat.ts` | 请求体增加 `conversation_id?: string`；若有则 createMessage(user)、流结束后 createMessage(assistant)、首条时 updateConversationTitle |
| 改造 ChatPage | 新建会话、选择会话、加载历史消息、发送时带 conversation_id |

**参考**：`.spec/IMPLEMENTATION_PLAN.md` 阶段二。

---

#### 2. RAG 接入 Chat

| 任务 | 说明 |
|------|------|
| Chat 请求体增加 `document_ids?: string[]` | 可选，限定检索范围 |
| 问题 embedding | 前端 `embed(question)` 或后端调用 embedding API；当前为本地模型，需前端 embed 后传向量 |
| 向量检索 | 后端 `searchChunks(embedding, { limit: 5 })`，调用 Supabase RPC `match_chunks` |
| 注入 context | 将 top K chunks 拼进 system prompt，再调 createChatStream |

**注意**：match_chunks 需为 vector(384)，与本地模型一致。若 DB 仍为 1536，需先迁移。

**参考**：`.spec/IMPLEMENTATION_PLAN.md` 阶段四。

---

#### 3. 数据库迁移（若未执行）

在 Supabase SQL Editor 执行：

```sql
TRUNCATE chunks;
ALTER TABLE chunks DROP COLUMN embedding;
ALTER TABLE chunks ADD COLUMN embedding vector(384) NOT NULL;

CREATE OR REPLACE FUNCTION match_chunks(query_embedding vector(384), match_count int DEFAULT 5)
RETURNS TABLE (id uuid, document_id uuid, content text, metadata jsonb)
LANGUAGE plpgsql AS $$
BEGIN
  RETURN QUERY
  SELECT c.id, c.document_id, c.content, c.metadata
  FROM chunks c
  ORDER BY c.embedding <=> query_embedding
  LIMIT match_count;
END;
$$;
```

---

### 3.2 中优先级（规范与体验）

#### 4. 国际化 (i18n)

| 任务 | 说明 |
|------|------|
| 引入 i18n 库 | react-i18next 或类似 |
| 创建 zh.json | 抽取所有 UI 文案 |
| 替换硬编码 | 使用 `t('key')` 替代中文 |
| 翻译脚本 | 监听 zh.json，调用 Gemini API 补全 en.json、ja.json |

**规范**：main.mdc 要求「严禁在 UI 层硬编码中文」。

---

#### 5. rem2px 适配

| 任务 | 说明 |
|------|------|
| 基准配置 | 1rem = 75px（750px 视觉稿） |
| 替换 px | 布局使用 rem，仅 1px 边框可用 px |
| 等比例缩放 | 支持移动端适配 |

**规范**：main.mdc 要求 rem2px 自动化。

---

#### 6. 双 Token 拦截（401）

| 任务 | 说明 |
|------|------|
| 引入 Axios | 替代 fetch（若需统一请求层） |
| 响应拦截器 | 捕获 401，触发 Token 刷新 |
| 请求队列 + 并发锁 | 刷新期间挂起请求，避免冗余调用 |

**规范**：main.mdc 要求双 Token 拦截。当前项目使用 fetch，若暂无 Auth，可延后。

---

### 3.3 低优先级（扩展功能）

#### 7. conversation_documents（可选）

- 前端：会话内「选择关联文档」
- 接口：POST /api/conversations/:id/documents，DELETE /api/conversations/:id/documents/:docId
- RAG：从 conversation_documents 取 document_ids，传给 match_chunks_filtered

---

#### 8. 溯源引用 (Citation)

| 任务 | 说明 |
|------|------|
| AI 回复携带源指针 | `[source_id, pointer]` |
| pointer 格式 | PDF 页码或视频时间戳 hh:mm:ss |
| 前端展示 | 引用来源可点击跳转 |

**规范**：main.mdc 要求溯源引用。

---

#### 9. 多格式解析扩展

| 格式 | 方案 | 状态 |
|------|------|------|
| PDF | MinerU / pdf-parse | 未实现 |
| DOCX | mammoth | 未实现 |
| 音频 | WhisperX | 有 test 脚本，未接入 |
| 视频 | 302.ai Video-Understanding | 有 test 脚本，未接入 |

---

#### 10. 自动向量化

- 上传完成后自动对 txt/md 发起向量化（当前需用户手动点击「向量化」）

---

## 四、实施顺序建议

| 顺序 | 内容 | 预估 |
|------|------|------|
| 1 | 挂载 conversations 路由 | 0.5 天 |
| 2 | Chat 接入 conversation_id + messages | 0.5–1 天 |
| 3 | RAG 接入 Chat（embed 问题 → 检索 → 注入 context） | 1–2 天 |
| 4 | 数据库迁移（若 embedding 仍为 1536） | 0.5 天 |
| 5 | 国际化 i18n | 1–2 天 |
| 6 | rem2px、双 Token 拦截 | 按需 |
| 7 | conversation_documents、溯源引用、多格式解析 | 后续迭代 |

---

## 五、文件结构速览

```
client/src/
├── components/     Layout, DocumentList, MultiFileUploadZone, UploadZone
├── hooks/          useEmbeddingModel, useMultiFileUpload, useFileUpload
├── lib/            embeddingClient, vectorizeService, uploadCore
├── pages/          ChatPage, DocumentsPage
├── workers/        hash.worker, embedding.worker
├── constants/      upload.ts
└── types/          (如有)

server/src/
├── lib/            supabase.ts
├── services/       documentService, chunkService, parseService,
│                   conversationService, messageService, aiProvider
├── routes/         chat, documents  （缺 conversations）
├── parsers/        textParser
└── constants/      upload.ts
```

---

## 六、验证清单

- [x] POST /api/documents/upload 能上传并写入 documents
- [x] GET /api/documents 能列出文档
- [x] txt/md 文档可解析、向量化、写入 chunks
- [ ] POST /api/conversations 创建会话
- [ ] GET /api/conversations/:id/messages 能拉消息
- [ ] Chat 带 conversation_id 时，messages 表有记录
- [ ] 有 chunks 后，Chat 能基于 RAG 返回检索增强回复
- [ ] UI 使用 t('key') 无硬编码中文
- [ ] rem2px 适配完成
